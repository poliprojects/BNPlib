TODO LIST

Elena:
* (print functions: remove last empty column)
* RNG seed {20200227} or  {std::random_device}
* eval_density + eval_density_from_states
* SerializeCollector: si salva gli oggetti serializzati (serializeToString)

Bruno:
* HierarchyNW
* sposta Factory in runtime
* Sistema proto: IterationOutput -> State
* .bin -> .recordio
* BaseCollector: how_many_states(), next() invece che
  collector.get_chains(iter), current_iter (chiamato da get_chains())

FEATURES/IMPLEMENTATION
* one method for prob_existing_cluster / prob_new_cluster?
* Dummy non funziona perché probas non vanno bene, è normale?
* store card vector somewhere?
* deal with the extra dynamically allocated Hypers (?)
* a way to erase and add clusters more efficiently?
* check, in write_clust_...(), if clust_estimate() has been called, and similar
  flags
* interfaccia Python (pybind) (una volta che ho catene di proto do in pasto a
  python, R..)

MANOVALANZA
* documentation with Doxygen
* complete README (VM etc)

REPORT
* segnala che i parametri non nel costruttore solitamente sono lasciati in
  default

TODOS DA MARIO
* run_neal2 che restituisce proto serializzati
** eval_density() static member?
** spesso prima sono fatte girare le catene, poi separatamente si fa eval_density()
* Diversi mains: neal2, neal8, stimedidensita
